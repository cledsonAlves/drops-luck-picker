export interface ParticipantResponse {
  sorteio: {
    BOOL: boolean;
  };
  email: {
    S: string;
  };
  name: {
    S: string;
  };
  photoUrl: {
    S: string;
  };
  userId: {
    S: string;
  };
  timestamp: {
    S: string;
  };
}